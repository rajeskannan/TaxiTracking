#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../forever-win/bin/foreverd" "$@"
  ret=$?
else 
  node  "$basedir/../forever-win/bin/foreverd" "$@"
  ret=$?
fi
exit $ret
