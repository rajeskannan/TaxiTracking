#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../i18nfp/bin/i18nfp" "$@"
  ret=$?
else 
  node  "$basedir/../i18nfp/bin/i18nfp" "$@"
  ret=$?
fi
exit $ret
