#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../xlsx/bin/xlsx2csv.njs" "$@"
  ret=$?
else 
  node  "$basedir/../xlsx/bin/xlsx2csv.njs" "$@"
  ret=$?
fi
exit $ret
